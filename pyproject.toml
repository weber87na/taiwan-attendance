[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "taiwan-attendance"
version = "0.1.0"
description = "繁體中文出勤打卡、假勤簽核及台灣法規試算參考實作"
requires-python = ">=3.12"
dependencies = []

[tool.setuptools]
packages = ["attendance"]
